fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
numpy>=2.0
scipy>=1.14
astropy>=7.0
astroquery>=0.4.11
matplotlib>=3.10
reportlab>=4.4
pydantic>=2.10
pytest>=8.3
# Microlensing (Module A) — PSPL fits use the closed form; MulensModel is
# listed so higher-order effects (parallax, finite-source, binary lens) can
# be dropped in without a re-install. The public Model API used here is
# stable across 2.x and 3.x.
MulensModel>=2.14; python_version >= "3.9"
# Microlensing (Module B) — TESS RA/Dec → (sector, camera, ccd).
tess-point>=0.9.4
